-- Vim-TMUX-Navigator plugin

return {
	'christoomey/vim-tmux-navigator',
	--event = "VeryLazy",
	--config = function() 
	--	local vim_tmux_navigator = require "vim-tmux-navigator"
	--	vim_tmux_navigator.setup {
	--		disable_when_zoomed = true, -- defaults to false
	--		keybindings = {
	--			left = "<C-h>",
	--			down = "<C-j>",
	--			up = "<C-k>",
	--			right = "<C-l>",
	--			last_active = "<C-\\>",
	--			next = "<C-Space>",
	--		},
	--	}
	--end,
}
